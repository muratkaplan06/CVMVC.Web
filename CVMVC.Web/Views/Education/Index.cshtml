@using CVMVC.Web.Models.Entity
@model List<Educations>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container mt-3">
    <div class="row">
        <div class="col-12">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">SUBTITLE1</th>
                        <th scope="col">SUBTITLE2</th>
                        <th scope="col">GPA</th>
                        <th scope="col">DATE</th>
                        <th scope="col">DELETE</th>
                        <th scope="col">UPDATE</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.ID</td>
                            <td>@item.subtitle1</td>
                            <td>@item.subtitle2</td>
                            <td>@item.gpa</td>
                            <td>@item.date</td>
                            <td>
                                <a href="/Education/DeleteEducation/@item.ID" class="btn btn-danger">
                                    <i class="fa fa-trash"></i>
                                    delete
                                </a>
                            </td>
                            <td>
                                <a href="/Education/UpdateEducation/@item.ID" class="btn btn-primary">
                                    <i class="fas fa-pencil-alt"></i>
                                    update
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <a href="/Education/AddEducation/" class="btn btn-primary">
        <i class="fas fa-plus" style="color: #ffffff;"></i>
    </a>

</div>
